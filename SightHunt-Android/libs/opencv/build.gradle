apply plugin: 'android-library'

buildscript {
    repositories {
        mavenCentral()
    }
}

android {
    compileSdkVersion 14
    buildToolsVersion "20.0.0"

    defaultConfig {
        minSdkVersion 8
        targetSdkVersion 19
        versionCode 2480
        versionName "2.4.8"
    }
}